[package]
name = "rhtn-cli"
version = "0.1.0"
edition = "2024"
description = "rhtn: the developer command line — decode what the wire carries, mint and inspect identities, and probe a running node"

[[bin]]
name = "rhtn"
path = "src/main.rs"

[dependencies]
rhtn-codec = { path = "../codec" }
rhtn-crypto = { path = "../crypto" }
rhtn-archive = { path = "../archive" }
rhtn-node = { path = "../node" }
rhtn-transport = { path = "../transport" }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "sync", "io-util"] }
quinn = { version = "0.11", default-features = false, features = ["runtime-tokio", "rustls-aws-lc-rs", "log"] }
