\* TLC configuration for CurrencyEscalation.tla.
\* One subject, one patron, two siblings, one grandpatron -- the smallest
\* instance in which all three rungs of the ladder are distinct.

SPECIFICATION Spec

CONSTANTS
  Subject = s0
  Patron = p0
  Siblings = {b1, b2}
  Grandpatron = g0
  LIFETIME = 3
  MaxClock = 6

INVARIANTS
  TypeOK
  FreshOnly
  SomeIssuerCanAct

PROPERTIES
  LadderMakesProgress
